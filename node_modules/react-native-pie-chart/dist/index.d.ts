/// <reference types="react" />
import { StyleProp, ViewStyle } from 'react-native';
export declare type Props = {
    widthAndHeight: number;
    series: number[];
    sliceColor: string[];
    coverFill?: string;
    coverRadius?: number;
    doughnut?: boolean;
    style?: StyleProp<ViewStyle>;
};
declare const PieChart: ({ widthAndHeight, series, sliceColor, coverFill, coverRadius, doughnut, style, }: Props) => JSX.Element;
export default PieChart;
